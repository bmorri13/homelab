services:
  openvas:
    image: immauss/openvas
    ports:
      - "9392:9392"
    environment:
      - USERNAME=${GVM_ADMIN_USER:-admin}
      - PASSWORD=${GVM_ADMIN_PASSWORD:-admin}
    volumes:
      - openvas_data:/data
    restart: unless-stopped

volumes:
  openvas_data:
