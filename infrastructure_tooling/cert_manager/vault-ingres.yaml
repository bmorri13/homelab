---
apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: vault
  annotations:
    metallb.universe.tf/address-pool: metallb-default
spec:
  type: LoadBalancer
  selector:
    app: vault
  ports:
  - name: https
    protocol: TCP
    port: 443
    targetPort: 8200
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vault
  namespace: vault
  annotations:
    cert-manager.io/cluster-issuer: bmosan-cert
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
  labels:
    app: vault
spec:
  ingressClassName: traefik
  rules:
  - host: vault.bmosan.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vault
            port:
              name: https
  tls:
  - hosts:
    - vault.bmosan.com
    secretName: vault-com
